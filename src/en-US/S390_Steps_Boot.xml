<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "Installation_Guide.ent">
%BOOK_ENTITIES;
]>

<chapter id="chap-installer-booting-ipl-s390">
	<title>Booting the Installation on IBM System&nbsp;z</title>
	<para>
		The steps to perform the initial program boot (IPL) of the <application>Anaconda</application> installation program depend on the environment (either z/VM or LPAR) in which &PRODUCT; will run.
	</para>

	<section id="sect-customizing-generic-prm-s390">
		<title>Customizing boot parameters</title>
		<para>
			Before you can begin the installation, you must configure some mandatory boot parameters. When installing through z/VM, these parameters must be configured before you boot in the <filename>generic.prm</filename> file. When installing on an LPAR, the <option>rd.cmdline</option> parameter is set to <literal>ask</literal> by default, meaning that you will be given a prompt on which you can enter these boot parameters. In both cases, the required parameters are the same.
		</para>
		<note>
			<para>
				Unlike Red&nbsp;Hat Enterprise Linux 6, which featured an interactive utility to assist network configuration, all network configuration must now be specified by the use of the following parameters, either by using a parameter file, or at the prompt.
			</para>
		</note>
		<variablelist>
			<varlistentry>
				<term>Installation source</term>
				<listitem>
					<para>
						An installation source must always be configured. Use the <option>inst.repo=</option> option to specify the package source for the installation. See <xref linkend="list-boot-options-sources" /> for details and syntax.
					</para>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term>Network devices</term>
				<listitem>
					<para>
						Network configuration must be provided if network access will be required during the installation. If you plan to perform an unattended (Kickstart-based) installation using only local media such as a hard drive, network configuration can be omitted.
					</para>
					<para>
						Use the <option>ip=</option> option for basic network configuration, and other options listed in <xref linkend="list-boot-options-network" /> as required.
					</para>
					<para>
						Also use the <option>rd.znet=</option> kernel option, which takes a network protocol type, a comma delimited list of sub-channels, and, optionally, comma delimited <parameter>sysfs</parameter> parameter and value pairs. This parameter can be specified multiple times to activate multiple network devices.
					</para>
					<para>
						For example:
					</para>
<screen>
rd.znet=qeth,0.0.0600,0.0.0601,0.0.0602,layer2=1,portname=foo
rd.znet=ctc,0.0.0600,0.0.0601,protocol=bar
</screen>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term>Storage devices</term>
				<listitem>
					<para>
						At least one storage device must always be configured.
					</para>
					<para>
						The <option>rd.dasd=</option> option takes a Direct Access Storage Device (DASD) adapter device bus identifier and, optionally, comma separated <parameter>sysfs</parameter> parameter and value pairs, then activates the device. This parameter can be specified multiple times to activate multiple DASDs. Example:
					</para>
<screen>
rd.dasd=0.0.0200,readonly=0
rd.dasd=0.0.0202,readonly=0
</screen>
					<para>
						The <option>rd.zfcp=</option> option takes a SCSI over FCP (zFCP) adapter device bus identifier, a world wide port name (WWPN), and a FCP LUN, then activates the device. This parameter can be specified multiple times to activate multiple zFCP devices. Example:
					</para>
<screen>rd.zfcp=0.0.4000,0x5005076300C213e9,0x5022000000000000</screen>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term>Kickstart options</term>
				<listitem>
					<para>
						If you are using a Kickstart file to perform an automatic installation, you must always specify the location of the Kickstart file using the <option>inst.ks=</option> option. For an unattended, fully automatic Kickstart installation, the <option>inst.cmdline</option> option is also useful. See <xref linkend="sect-parameter-configuration-files-kickstart-s390" /> for additional information.
					</para>
				</listitem>
			</varlistentry>
		</variablelist>
		<para>
			An example customized <filename>generic.prm</filename> file containing all mandatory parameters look similar to the following example:
		</para>
		<example id="exam-modified-generic-prm">
			<title>Customized generic.prm file</title>
<screen>ro ramdisk_size=40000 cio_ignore=all,!condev
inst.repo=http://example.com/path/to/repository
rd.znet=qeth,0.0.0600,0.0.0601,0.0.0602,layer2=1,portno=0,portname=foo
ip=192.168.17.115::192.168.17.254:24:foobar.systemz.example.com:enccw0.0.0600:none
nameserver=192.168.17.1
rd.dasd=0.0.0200 rd.dasd=0.0.0202
rd.zfcp=0.0.4000,0x5005076300C213e9,0x5022000000000000
inst.ks=http://example.com/path/to/kickstart</screen>
			</example>
			<para>
				Some installation methods also require a file with a mapping of the location of installation data in the file system of the DVD or FTP server and the memory locations where the data is to be copied. The file is typically named <filename>generic.ins</filename>, and contains file names for the initial RAM disk, kernel image, and parameter file (<filename>generic.prm</filename>) and a memory location for each file. An example <filename>generic.ins</filename> will look similar to the following example:
			</para>
			<example id="exam-generic-ins">
				<title>Sample generic.ins file</title>
<screen>images/kernel.img 0x00000000
images/initrd.img 0x02000000
images/genericdvd.prm 0x00010480
images/initrd.addrsize 0x00010408</screen>
				</example>
				<para>
					A valid <filename>generic.ins</filename> file is provided by Red&nbsp;Hat along with all other files required to boot the installer. Modify this file only if you want to, for example, load a different kernel version than default.
				</para>
			</section>

			<section id="sect-installation-source-hdd-s390x">
				<title>Considerations for Hard Drive Installation on IBM System&nbsp;z</title>
				<para>
					If you want to boot the installation program from a hard drive,  you can optionally install the <application>zipl</application> boot loader on the same (or a different) disk. Be aware that <application>zipl</application> only supports one boot record per disk. If you have multiple partitions on a disk, they all "share" the disk's single boot record.
				</para>
				<para>
					To prepare a hard drive to boot the installation program, install the <application>zipl</application> boot loader on the hard drive by entering the following command:
				</para>
<screen>
<prompt>#</prompt> <command>zipl -V -t /mnt/ -i /mnt/images/kernel.img -r /mnt/images/initrd.img -p /mnt/images/generic.prm</command>
</screen>

				<para>
					See <xref linkend="sect-customizing-generic-prm-s390" /> for details on customizing boot parameters in the <filename>generic.prm</filename> configuration file.
				</para>
			</section>

			<section id="sect-installing-zvm-s390">
				<title>Installing under z/VM</title>
				<para>
					When installing under z/VM, you can boot from:
				</para>
				<itemizedlist>
					<listitem>
						<para>
							the z/VM virtual reader
						</para>
					</listitem>
					<listitem>
						<para>
							a DASD or an FCP-attached SCSI device prepared with the <application>zipl</application> boot loader
						</para>
					</listitem>
					<listitem>
						<para>
							an FCP-attached SCSI DVD drive
						</para>
					</listitem>
				</itemizedlist>
				<para>
					Log on to the z/VM guest virtual machine chosen for the Linux installation. You can use the <application>x3270</application> or <application>c3270</application> terminal emulator, available in the <package>x3270-text</package> package in &PRODUCT;, to log in to z/VM from other Linux systems. Alternatively, use the IBM 3270 terminal emulator on the IBM System&nbsp;z Hardware Management Console (HMC). If you are working from a machine with a Microsoft Windows operating system, Jolly Giant (<ulink url="http://www.jollygiant.com/" />) offers an SSL-enabled 3270 emulator. A free native Windows port of <application>c3270</application> called <application>wc3270</application> also exists.
				</para>
				<note>
					<para>
						If your 3270 connection is interrupted and you cannot log in again because the previous session is still active, you can replace the old session with a new one by entering the following command on the z/VM logon screen:
					</para>

<screen><command>logon <replaceable>user</replaceable> here</command></screen>

					<para>
						Replace <replaceable>user</replaceable> with the name of the z/VM guest virtual machine. Depending on whether an external security manager, for example RACF, is used, the logon command might vary.
					</para>
				</note>
				<para>
					If you are not already running <application>CMS</application> (single-user operating system shipped with z/VM) in your guest, boot it now by entering the command:
				</para>

<screen><command>cp ipl cms</command></screen>

				<para>
					Be sure not to use CMS disks such as your A disk (often device number 0191) as installation targets. To find out which disks are in use by CMS, use the following query:
				</para>

<screen><command>query disk</command></screen>

				<para>
					You can use the following CP (z/VM Control Program, which is the z/VM hypervisor) query commands to find out about the device configuration of your z/VM guest virtual machine:
				</para>
				<itemizedlist>
					<listitem>
						<para>
							Query the available main memory, which is called <firstterm>storage</firstterm> in System&nbsp;z terminology. Your guest should have at least 1 GB  of main memory.
						</para>

<screen><command>cp query virtual storage</command></screen>

					</listitem>
					<listitem>
						<para>
							Query available network devices by type:
						</para>
						<variablelist>
							<varlistentry>
								<term><command>osa</command></term>
								<listitem>
									<para>
										OSA - CHPID type OSD, real or virtual (VSWITCH or GuestLAN), both in QDIO mode
									</para>
								</listitem>
							</varlistentry>
							<varlistentry>
								<term><command>hsi</command></term>
								<listitem>
									<para>
										HiperSockets - CHPID type IQD, real or virtual (GuestLAN type Hipers)
									</para>
								</listitem>
							</varlistentry>
							<varlistentry>
								<term><command>lcs</command></term>
								<listitem>
									<para>
										LCS - CHPID type OSE
									</para>
								</listitem>
							</varlistentry>
						</variablelist>
						<para>
							For example, to query all of the network device types mentioned above, run:
						</para>

<screen><command>cp query virtual osa</command></screen>

					</listitem>
					<listitem>
						<para>
							Query available DASDs. Only those that are flagged <systemitem>RW</systemitem> for read-write mode can be used as installation targets:
						</para>

<screen><command>cp query virtual dasd</command></screen>

					</listitem>
					<listitem>
						<para>
							Query available FCP channels:
						</para>

<screen><command>cp query virtual fcp</command></screen>

					</listitem>
				</itemizedlist>

				<section id="sect-installing-zvm-reader-s390">
					<title>Using the z/VM Reader</title>
					<para>
						Perform the following steps to boot from the z/VM reader:
					</para>
					<procedure>
						<step>
							<para>
								If necessary, add the device containing the z/VM TCP/IP tools to your CMS disk list. For example:
							</para>
<screen>
cp link tcpmaint 592 592
acc 592 <replaceable>fm</replaceable>
</screen>
							<para>
								Replace <replaceable>fm</replaceable> with any <systemitem>FILEMODE</systemitem> letter.
							</para>
						</step>
						<step>
							<para>
								Execute the command:
							</para>

<screen><command>ftp <replaceable>host</replaceable></command></screen>

							<para>
								Where <replaceable>host</replaceable> is the host name or IP address of the FTP server that hosts the boot images (<filename>kernel.img</filename> and <filename>initrd.img</filename>).
							</para>
						</step>
						<step>
							<para>
								Log in and execute the following commands. Use the <option>(repl</option> option if you are overwriting existing <filename>kernel.img</filename>, <filename>initrd.img</filename>, <filename>generic.prm</filename>, or <filename>redhat.exec</filename> files:
							</para>

<screen>
<command>cd <replaceable>/location/of/install-tree</replaceable>/images/</command>
<command>ascii </command>
<command>get generic.prm (repl </command>
<command>get redhat.exec (repl </command>
<command>locsite fix 80 </command>
<command>binary </command>
<command>get kernel.img (repl </command>
<command>get initrd.img (repl </command>
<command>quit</command>
</screen>
						</step>
						<step>
							<para>
								Optionally, check whether the files were transferred correctly by using the CMS command <command>filelist</command> to show the received files and their format. It is important that <filename>kernel.img</filename> and <filename>initrd.img</filename> have a fixed record length format denoted by <literal>F</literal> in the Format column and a record length of 80 in the <literal>Lrecl</literal> column. For example:
							</para>

<screen>
VMUSER FILELIST A0 V 169 Trunc=169 Size=6 Line=1 Col=1 Alt=0
Cmd Filename	Filetype	Fm	Format	Lrecl	Records	Blocks	Date	Time
REDHAT	EXEC		B1	V	22	1 	1	4/15/10	9:30:40
GENERIC	PRM		B1	V	44	1	1	4/15/10	9:30:32
INITRD	IMG		B1	F	80	118545	2316	4/15/10	9:30:25
KERNEL	IMG		B1	F	80	74541	912	4/15/10	9:30:17
</screen>

							<para>
								Press <keycap>PF3</keycap> to quit <command>filelist</command> and return to the CMS prompt.
							</para>
						</step>
						<step>
							<para>
								Customize boot parameters in <filename>generic.prm</filename> as necessary. See <xref linkend="sect-customizing-generic-prm-s390" /> for details.
							</para>
							<para>
								Another way to configure storage and network devices is by using a CMS configuration file. In such a case, add the <parameter>CMSDASD=</parameter> and <parameter>CMSCONFFILE=</parameter> parameters to <filename>generic.prm</filename>. See <xref linkend="sect-parameter-configuration-files-zvm-s390" /> for more details.
							</para>
						</step>
						<step>
							<para>
								Finally, execute the REXX script <filename>redhat.exec</filename> to boot the installation program:
							</para>
<screen><command>redhat</command></screen>
						</step>
					</procedure>
				</section>

				<section id="sect-installing-zvm-prepared-dasd-s390">
					<title>Using a Prepared DASD</title>
					<para>
						Boot from the prepared DASD and select the <application>zipl</application> boot menu entry referring to the &PRODUCT; installation program. Use a command of the following form:
					</para>

<screen><command>cp ipl <replaceable>DASD_device_number</replaceable> loadparm <replaceable>boot_entry_number</replaceable></command></screen>

					<para>
						Replace <replaceable>DASD_device_number</replaceable> with the device number of the boot device, and <replaceable>boot_entry_number</replaceable> with the <application>zipl</application> configuration menu for this device. For example:
					</para>

<screen><command>cp ipl eb1c loadparm 0</command></screen>

				</section>

				<section id="sect-installing-zvm-prepared-fcp-scsi-s390">
					<title>Using a Prepared FCP-attached SCSI Disk</title>
					<para>
						Perform the following steps to boot from a prepared FCP-attached SCSI disk:
					</para>
					<procedure>
						<step>
							<para>
								Configure the SCSI boot loader of z/VM to access the prepared SCSI disk in the FCP Storage Area Network. Select the prepared <application>zipl</application> boot menu entry referring to the &PRODUCT; installation program. Use a command of the following form:
							</para>

<screen><command>cp set loaddev portname <replaceable>WWPN</replaceable> lun <replaceable>LUN</replaceable> bootprog <replaceable>boot_entry_number</replaceable></command></screen>

							<para>
								Replace <replaceable>WWPN</replaceable> with the World Wide Port Name of the storage system and <replaceable>LUN</replaceable> with the Logical Unit Number of the disk. The 16-digit hexadecimal numbers must be split into two pairs of eight digits each. For example:
							</para>

<screen><command>cp set loaddev portname 50050763 050b073d lun 40204011 00000000 bootprog 0</command></screen>

						</step>
						<step>
							<para>
								Optionally, confirm your settings with the command:
							</para>

<screen><command>query loaddev</command></screen>

						</step>
						<step>
							<para>
								Boot the FCP device connected with the storage system containing the disk with the following command:
							</para>

<screen><command>cp ipl <replaceable>FCP_device</replaceable> </command></screen>

							<para>
								For example:
							</para>

<screen><command>cp ipl fc00</command></screen>

						</step>
					</procedure>
				</section>

				<section id="sect-installing-zvm-prepared-fcp-dvd-s390">
					<title>Using an FCP-attached SCSI DVD Drive</title>
					<para>
						This requires a SCSI DVD drive attached to an FCP-to-SCSI bridge which is in turn connected to an FCP adapter in your System&nbsp;z. The FCP adapter must be configured and available under z/VM.
					</para>
					<procedure>
						<step>
							<para>
								Insert your &PRODUCT; for System&nbsp;z DVD into the DVD drive.
							</para>
						</step>
						<step>
							<para>
								Configure the SCSI boot loader of z/VM to access the DVD drive in the FCP Storage Area Network and specify <literal>1</literal> for the boot entry on the &PRODUCT; for System&nbsp;z DVD. Use a command of the following form:
							</para>
<screen><command>cp set loaddev portname <replaceable>WWPN</replaceable> lun <replaceable>FCP_LUN</replaceable> bootprog 1</command></screen>
							<para>
								Replace <replaceable>WWPN</replaceable> with the WWPN of the FCP-to-SCSI bridge and <replaceable>FCP_LUN</replaceable> with the LUN of the DVD drive. The 16-digit hexadecimal numbers must be split into two pairs of eight characters each. For example:
							</para>
<screen><command>cp set loaddev portname 20010060 eb1c0103 lun 00010000 00000000 bootprog 1</command></screen>
						</step>
						<step>
							<para>
								Optionally, confirm your settings with the command:
							</para>
<screen><command>cp query loaddev</command></screen>
						</step>
						<step>
							<para>
								IPL on the FCP device connected with the FCP-to-SCSI bridge.
							</para>
<screen><command>cp ipl <replaceable>FCP_device</replaceable></command></screen>
							<para>
								For example:
							</para>
<screen><command>cp ipl fc00</command></screen>
						</step>
					</procedure>
				</section>

			</section>

			<section id="sect-installing-lpar-s390">
				<title>Installing in an LPAR</title>
				<para>
					When installing in a <firstterm>logical partition</firstterm> (LPAR), you can boot from:
				</para>
				<itemizedlist>
					<listitem>
						<para>
							an FTP server
						</para>
					</listitem>
					<listitem>
						<para>
							a DASD or an FCP-attached SCSI drive prepared with the <application>zipl</application> boot loader
						</para>
					</listitem>
					<listitem>
						<para>
							an FCP-attached SCSI DVD drive
						</para>
					</listitem>
				</itemizedlist>

				<para>
					Perform these common steps first:
				</para>
				<procedure>
					<step>
						<para>
							Log in on the IBM System&nbsp;z <firstterm>Hardware Management Console</firstterm> (HMC) or the <firstterm>Support Element</firstterm> (SE) as a user with sufficient privileges to install a new operating system to an LPAR. The <systemitem>SYSPROG</systemitem> user is recommended.
						</para>
					</step>
					<step>
						<para>
							Select <guilabel>Images</guilabel>, then select the LPAR to which you want to install. Use the arrows in the frame on the right side to navigate to the <guimenu>CPC Recovery</guimenu> menu.
						</para>
					</step>
					<step>
						<para>
							Double-click <guilabel>Operating System Messages</guilabel> to show the text console on which Linux boot messages will appear.
						</para>
					</step>
				</procedure>
				<para>
					Continue with the procedure for your installation source.
				</para>
				<note>
					<para>
						Once you finish this procedure and one of the following ones depending on your installation source, the installation will begin. The installer will then prompt you to provide additional boot parameters. Required parameters are described in <xref linkend="sect-customizing-generic-prm-s390" />.
					</para>
				</note>

				<section id="sect-installing-lpar-ftp-s390">
					<title>Using an FTP Server</title>
					<procedure>
						<step>
							<para>
								Double-click <guilabel>Load from CD-ROM, DVD, or Server</guilabel>.
							</para>
						</step>
						<step>
							<para>
								In the dialog box that follows, select <guilabel>FTP Source</guilabel>, and enter the following information:

								<itemizedlist>
									<listitem>
										<para>
											<guilabel>Host Computer</guilabel> - Host name or IP address of the FTP server you want to install from, for example <literal>ftp.redhat.com</literal>
										</para>
									</listitem>
									<listitem>
										<para>
											<guilabel>User ID</guilabel> - Your user name on the FTP server. Or, specify <literal>anonymous</literal>.
										</para>
									</listitem>
									<listitem>
										<para>
											<guilabel>Password</guilabel> - Your password. Use your email address if you are logging in as <literal>anonymous</literal>.
										</para>
									</listitem>
									<listitem>
										<para>
											<guilabel>Account (optional)</guilabel> - Leave this field empty.
										</para>
									</listitem>
									<listitem>
										<para>
											<guilabel>File location (optional)</guilabel> - Directory on the FTP server holding the &PRODUCT; for System&nbsp;z, for example <filename class="directory">/rhel/s390x/</filename>.
										</para>
									</listitem>
								</itemizedlist>

							</para>
						</step>
						<step>
							<para>
								Click <guilabel>Continue</guilabel>.
							</para>
						</step>
						<step>
							<para>
								In the dialog that follows, keep the default selection of <literal>generic.ins</literal> and click <guilabel>Continue</guilabel>.
							</para>
						</step>
					</procedure>
				</section>

				<section id="sect-installing-lpar-dasd-s390">
					<title>Using a Prepared DASD</title>
					<procedure>
						<step>
							<para>
								Double-click <guilabel>Load</guilabel>.
							</para>
						</step>
						<step>
							<para>
								In the dialog box that follows, select <literal>Normal</literal> as the <guilabel>Load type</guilabel>.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>Load address</guilabel>, fill in the device number of the DASD.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>Load parameter</guilabel>, fill in the number corresponding the <application>zipl</application> boot menu entry that you prepared for booting the &PRODUCT; installation program.
							</para>
						</step>
						<step>
							<para>
								Click the <guibutton>OK</guibutton> button.
							</para>
						</step>
					</procedure>
				</section>

				<section id="sect-installing-lpar-fcp-scsi-s390">
					<title>Using a Prepared FCP-attached SCSI Disk</title>
					<procedure>
						<step>
							<para>
								Double-click <guilabel>Load</guilabel>.
							</para>
						</step>
						<step>
							<para>
								In the dialog box that follows, select <literal>SCSI</literal> as the <guilabel>Load type</guilabel>.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>Load address</guilabel>, fill in the device number of the FCP channel connected with the SCSI disk.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>World wide port name</guilabel>, fill in the WWPN of the storage system containing the disk as a 16-digit hexadecimal number.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>Logical unit number</guilabel>, fill in the LUN of the disk as a 16-digit hexadecimal number.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>Boot program selector</guilabel>, fill in the number corresponding the <application>zipl</application> boot menu entry that you prepared for booting the &PRODUCT; installation program.
							</para>
						</step>
						<step>
							<para>
								Leave the <guilabel>Boot record logical block address</guilabel> as <literal>0</literal> and the <guilabel>Operating system specific load parameters</guilabel> empty.
							</para>
						</step>
						<step>
							<para>
								Click the <guibutton>OK</guibutton> button.
							</para>
						</step>
					</procedure>
				</section>

				<section id="sect-installing-lpar-fcp-dvd-s390">
					<title>Using an FCP-attached SCSI DVD Drive</title>
					<para>
						This requires a SCSI DVD drive attached to an FCP-to-SCSI bridge which is in turn connected to an FCP adapter in your System&nbsp;z machine. The FCP adapter must be configured and available in your LPAR.
					</para>
					<procedure>
						<step>
							<para>
								Insert your &PRODUCT; for System&nbsp;z DVD into the DVD drive.
							</para>
						</step>
						<step>
							<para>
								Double-click <guilabel>Load</guilabel>.
							</para>
						</step>
						<step>
							<para>
								In the dialog box that follows, select <literal>SCSI</literal> as the <guilabel>Load type</guilabel>.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>Load address</guilabel>, fill in the device number of the FCP channel connected with the FCP-to-SCSI bridge.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>World wide port name</guilabel>, fill in the WWPN of the FCP-to-SCSI bridge as a 16-digit hexadecimal number.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>Logical unit number</guilabel>, fill in the LUN of the DVD drive as a 16-digit hexadecimal number.
							</para>
						</step>
						<step>
							<para>
								As <guilabel>Boot program selector</guilabel>, fill in the number <literal>1</literal> to select the boot entry on the &PRODUCT; for System&nbsp;z DVD.
							</para>
						</step>
						<step>
							<para>
								Leave the <guilabel>Boot record logical block address</guilabel> as <literal>0</literal> and the <guilabel>Operating system specific load parameters</guilabel> empty.
							</para>
						</step>
						<step>
							<para>
								Click the <guibutton>OK</guibutton> button.
							</para>
						</step>
					</procedure>
				</section>
			</section>
		</chapter>
