<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "Installation_Guide.ent">
%BOOK_ENTITIES;
]>

<chapter id="chap-parameter-configuration-files-s390">
	<title>Parameter and Configuration Files on IBM System&nbsp;z</title>


	<indexterm>
		<primary>parameter files</primary>

	</indexterm>
	<indexterm>
		<primary>parm files</primary>
		<see>parameter files</see>
	</indexterm>
	<indexterm>
		<primary>CMS configuration files</primary>
	</indexterm>
	<indexterm>
		<primary>configuration files</primary>
		<secondary>CMS configuration files</secondary>
	</indexterm>


	<para>
		The IBM System&nbsp;z architecture can use a customized parameter file to pass boot parameters to the kernel and the installation program. This section describes the contents of this parameter file.
	</para>
	<para>
		You need only read this section if you intend to change the shipped parameter file. You need to change the parameter file if you want to:
	</para>
	<itemizedlist>
		<listitem>
			<para>
				install unattended with Kickstart.
			</para>

		</listitem>
		<listitem>
			<para>
				choose non-default installation settings that are not accessible through the installation program"s interactive user interface, such as rescue mode.
			</para>

		</listitem>

	</itemizedlist>
	<para>
		The parameter file can be used to set up networking non-interactively before the installation program (loader and <application>Anaconda</application>) starts.
	</para>
	<para>
		The kernel parameter file is limited to 895 characters plus an end-of-line character. The parameter file can be variable or fixed record format. Fixed record format increases the file size by padding each line up to the record length. Should you encounter problems with the installation program not recognizing all specified parameters in LPAR environments, you can try to put all parameters in one single line or start and end each line with a space character.
	</para>
	<para>
		The parameter file contains kernel parameters, such as <literal>ro</literal>, and parameters for the installation process, such as <literal>vncpassword=test</literal> or <literal>vnc</literal>.
	</para>
	<section id="sect-parameter-configuration-files-required-s390">
		<title>Required Parameters</title>
		<indexterm>
			<primary>parameter files</primary>
			<secondary>required parameters</secondary>

		</indexterm>
		<para>
			The following parameters are required and must be included in the parameter file. They are also provided in the file <filename>generic.prm</filename> in directory <filename>images/</filename> of the installation DVD:
		</para>
		<variablelist>
			<varlistentry>
				<term><parameter>ro</parameter></term>
				<listitem>
					<para>
						mounts the root file system, which is a RAM disk, read-only.
					</para>

				</listitem>
			</varlistentry>
			<varlistentry>
				<term><parameter>ramdisk_size=<replaceable>size</replaceable></parameter></term>
				<listitem>
					<para>
						modifies the memory size reserved for the RAM disk to ensure that the &PRODUCT; installation program fits within it. For example: <literal>ramdisk_size=40000</literal>.
					</para>

				</listitem>
			</varlistentry>

		</variablelist>
		<para>
			The <filename>generic.prm</filename> file also contains the additional parameter <literal>cio_ignore=all,!condev</literal>. This setting speeds up boot and device detection on systems with many devices. The installation program transparently handles the activation of ignored devices.
		</para>
		<important>
			<para>
				To avoid installation problems arising from <command>cio_ignore</command> support not being implemented throughout the entire stack, adapt the <parameter>cio_ignore=</parameter> parameter value to your system or remove the parameter entirely from your parameter file used for booting (IPL) the installation program.
			</para>
		</important>

	</section>

	<section id="sect-parameter-configuration-files-zvm-s390">
		<title>The z/VM Configuration File</title>
		<indexterm>
			<primary>configuration files</primary>
			<secondary>the z/VM configuration file</secondary>

		</indexterm>
		<para>
			This applies only if installing under z/VM. Under z/VM, you can use a configuration file on a CMS-formatted disk. The purpose of the CMS configuration file is to save space in the parameter file by moving the parameters that configure the initial network setup, the DASD, and the FCP specification out of the parameter file (see <xref linkend="sect-parameter-configuration-files-network-s390" />).
		</para>
		<para>
			Each line of the CMS configuration file contains a single variable and its associated value, in the following shell-style syntax: <literal> <replaceable>variable</replaceable>=<replaceable>value</replaceable> </literal>.
		</para>
		<para>
			You must also add the <parameter>CMSDASD</parameter> and <parameter>CMSCONFFILE</parameter> parameters to the parameter file. These parameters point the installation program to the configuration file:
		</para>
		<variablelist>
			<varlistentry>
				<term><parameter>CMSDASD=<replaceable>cmsdasd_address</replaceable> </parameter></term>
				<listitem>
					<para>
						Where <replaceable>cmsdasd_address</replaceable> is the device number of a CMS-formatted disk that contains the configuration file. This is usually the CMS user"s <systemitem>A</systemitem> disk.
					</para>
					<para>
						For example: <parameter>CMSDASD=191</parameter>
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>CMSCONFFILE=<replaceable>configuration_file</replaceable> </parameter></term>
				<listitem>
					<para>
						Where <replaceable>configuration_file</replaceable> is the name of the configuration file. This value must be specified in lower case. It is specified in a Linux file name format: <filename><replaceable>CMS_file_name</replaceable>.<replaceable>CMS_file_type</replaceable></filename>.
					</para>
					<para>
						The CMS file <filename>REDHAT CONF</filename> is specified as <filename>redhat.conf</filename>. The CMS file name and the file type can each be from one to eight characters that follow the CMS conventions.
					</para>
					<para>
						For example: <parameter>CMSCONFFILE=redhat.conf</parameter>
					</para>

				</listitem>

			</varlistentry>

		</variablelist>

	</section>

	<section id="sect-parameter-configuration-files-network-s390">
		<title>Installation Network Parameters</title>

		<indexterm>
			<primary>parameter files</primary>
			<secondary>installation network parameters</secondary>
		</indexterm>

		<para>
			The following parameters can be used to set up the preliminary network automatically and can be defined in  the CMS configuration file. The parameters in this section are the only parameters that can also be used in a CMS configuration file. All other parameters in other sections must be specified in the parameter file.
		</para>
		<variablelist>
			<varlistentry>
				<term><parameter>NETTYPE="<replaceable>type</replaceable>"</parameter> </term>
				<listitem>
					<para>
						Where <replaceable>type</replaceable> must be one of the following: <literal>qeth</literal>, <literal>lcs</literal>, or <literal>ctc</literal>. The default is <literal>qeth</literal>.
					</para>
					<para>
						Choose <literal>lcs</literal> for:
					</para>
					<itemizedlist>
						<listitem>
							<para>
								OSA-2 Ethernet/Token Ring
							</para>

						</listitem>
						<listitem>
							<para>
								OSA-Express Fast Ethernet in non-QDIO mode
							</para>

						</listitem>
						<listitem>
							<para>
								OSA-Express High Speed Token Ring in non-QDIO mode
							</para>

						</listitem>
						<listitem>
							<para>
								Gigabit Ethernet in non-QDIO mode
							</para>

						</listitem>

					</itemizedlist>
					<para>
						Choose <literal>qeth</literal> for:
					</para>
					<itemizedlist>
						<listitem>
							<para>
								OSA-Express Fast Ethernet
							</para>

						</listitem>
						<listitem>
							<para>
								Gigabit Ethernet (including 1000Base-T)
							</para>

						</listitem>
						<listitem>
							<para>
								High Speed Token Ring
							</para>

						</listitem>
						<listitem>
							<para>
								HiperSockets
							</para>

						</listitem>
						<listitem>
							<para>
								ATM (running Ethernet LAN emulation)
							</para>

						</listitem>

					</itemizedlist>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>SUBCHANNELS="<replaceable>device_bus_IDs</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>device_bus_IDs</replaceable> is a comma-separated list of two or three device bus IDs. The IDs must be specified in lowercase.
					</para>
					<para>
						Provides required device bus IDs for the various network interfaces:
					</para>

<screen>qeth: SUBCHANNELS="<replaceable>read_device_bus_id</replaceable>,<replaceable>write_device_bus_id</replaceable>,<replaceable>data_device_bus_id</replaceable>"
lcs or ctc: SUBCHANNELS="<replaceable>read_device_bus_id</replaceable>,<replaceable>write_device_bus_id</replaceable>"</screen>
					<para>
						For example (a sample qeth SUBCHANNEL statement):
					</para>

<screen>SUBCHANNELS="0.0.f5f0,0.0.f5f1,0.0.f5f2"</screen>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>PORTNAME="<replaceable>osa_portname</replaceable>" </parameter></term>
				<term><parameter>PORTNAME="<replaceable>lcs_portnumber</replaceable>" </parameter></term>
				<listitem>
					<para>
						This variable supports OSA devices operating in qdio mode or in non-qdio mode.
					</para>
					<para>
						When using qdio mode (<literal>NETTYPE="qeth"</literal>), <replaceable>osa_portname</replaceable> is the portname specified on the OSA device when operating in qeth mode.
					</para>
					<para>
						When using non-qdio mode (<literal>NETTYPE="lcs"</literal>), <replaceable>lcs_portnumber</replaceable> is used to pass the relative port number as a decimal integer in the range of 0 through 15.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>PORTNO="<replaceable>portnumber</replaceable>"</parameter></term>
				<listitem>
					<para>
						You can add either <literal>PORTNO="0"</literal> (to use port 0) or <literal>PORTNO="1"</literal> (to use port 1 of OSA features with two ports per CHPID) to the CMS configuration file to avoid being prompted for the mode.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>LAYER2="<replaceable>value</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>value</replaceable> can be <literal>0</literal> or <literal>1</literal>.
					</para>
					<para>
						Use <literal>LAYER2="0"</literal> to operate an OSA or HiperSockets device in layer 3 mode (<literal>NETTYPE="qeth"</literal>). Use <literal>LAYER2="1"</literal> for layer 2 mode. For virtual network devices under z/VM this setting must match the definition of the GuestLAN or VSWITCH to which the device is coupled.
					</para>
					<para>
						To use network services that operate on layer 2 (the Data Link Layer or its MAC sublayer) such as DHCP, layer 2 mode is a good choice.
					</para>
					<para>
						The qeth device driver default for OSA devices is now layer 2 mode. To continue using the previous default of layer 3 mode, set <literal>LAYER2="0"</literal> explicitly.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>VSWITCH="<replaceable>value</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>value</replaceable> can be <literal>0</literal> or <literal>1</literal>.
					</para>
					<para>
						Specify <literal>VSWITCH="1"</literal> when connecting to a z/VM VSWITCH or GuestLAN, or <literal>VSWITCH="0"</literal> (or nothing at all) when using directly attached real OSA or directly attached real HiperSockets.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>MACADDR="<replaceable>MAC_address</replaceable>"</parameter></term>
				<listitem>
					<para>
						If you specify <literal>LAYER2="1"</literal> and <literal>VSWITCH="0"</literal>, you can optionally use this parameter to specify a MAC address. Linux requires six colon-separated octets as pairs lower case hex digits - for example, <literal>MACADDR=62:a3:18:e7:bc:5f</literal>. Note that this is different from the notation used by z/VM.
					</para>
					<para>
						If you specify <literal>LAYER2="1"</literal> and <literal>VSWITCH="1"</literal>, you must not specify the <parameter>MACADDR</parameter>, because z/VM assigns a unique MAC address to virtual network devices in layer 2 mode.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>CTCPROT="<replaceable>value</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>value</replaceable> can be <literal>0</literal>, <literal>1</literal>, or <literal>3</literal>.
					</para>
					<para>
						Specifies the CTC protocol for <literal>NETTYPE="ctc"</literal>. The default is <literal>0</literal>.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>HOSTNAME="<replaceable>string</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>string</replaceable> is the host name of the newly-installed Linux instance.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>IPADDR="<replaceable>IP</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>IP</replaceable> is the IP address of the new Linux instance.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>NETMASK="<replaceable>netmask</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>netmask</replaceable> is the netmask.
					</para>
					<para>
						The netmask supports the syntax of a prefix integer (from 1 to 32) as specified in IPv4 <firstterm>classless interdomain routing</firstterm> (CIDR). For example, you can specify <literal>24</literal> instead of <literal>255.255.255.0</literal>, or <literal>20</literal> instead of <literal>255.255.240.0</literal>.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>GATEWAY="<replaceable>gw</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>gw</replaceable> is the gateway IP address for this network device.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>MTU="<replaceable>mtu</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where <replaceable>mtu</replaceable> is the <firstterm>Maximum Transmission Unit</firstterm> (MTU) for this network device.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>DNS="<replaceable>server1</replaceable>:<replaceable>server2</replaceable>:<replaceable>additional_server_terms</replaceable>:<replaceable>serverN</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where "<replaceable>server1</replaceable>:<replaceable>server2</replaceable>:<replaceable>additional_server_terms</replaceable>:<replaceable>serverN</replaceable>" is a list of DNS servers, separated by colons. For example:
					</para>

<screen>DNS="10.1.2.3:10.3.2.1"</screen>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>SEARCHDNS="<replaceable>domain1</replaceable>:<replaceable>domain2</replaceable>:<replaceable>additional_dns_terms</replaceable>:<replaceable>domainN</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where "<replaceable>domain1</replaceable>:<replaceable>domain2</replaceable>:<replaceable>additional_dns_terms</replaceable>:<replaceable>domainN</replaceable>" is a list of the search domains, separated by colons. For example:
					</para>

<screen>SEARCHDNS="subdomain.domain:domain"</screen>
					<para>
						You only need to specify <parameter>SEARCHDNS=</parameter> if you specify the <parameter>DNS=</parameter> parameter.
					</para>

				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>DASD=</parameter></term>
				<listitem>
					<para>
						Defines the DASD or range of DASDs to configure for the installation.
					</para>
					<para>
						The installation program supports a comma-separated list of device bus IDs or of ranges of device bus IDs with the optional attributes <option>ro</option>, <option>diag</option>, <option>erplog</option>, and <option>failfast</option>. Optionally, you can abbreviate device bus IDs to device numbers with leading zeros stripped. Any optional attributes should be separated by colons and enclosed in parentheses. Optional attributes follow a device bus ID or a range of device bus IDs.
					</para>
					<para>
						The only supported global option is <option>autodetect</option>. This does not support the specification of non-existent DASDs to reserve kernel device names for later addition of DASDs. Use persistent DASD device names (for example <filename>/dev/disk/by-path/...</filename>) to enable transparent addition of disks later. Other global options such as <option>probeonly</option>, <option>nopav</option>, or <option>nofcx</option> are not supported by the installation program.
					</para>
					<para>
						Only specify those DASDs that you really need to install your system. All unformatted DASDs specified here must be formatted after a confirmation later on in the installation program (see <xref linkend="sect-storage-device-selection-dasd-formatting-s390" />). Add any data DASDs that are not needed for the root file system or the <filename>/boot</filename> partition after installation as described in <xref linkend="sect-post-installation-dasds-no-root-s390" />.
					</para>
					<para>
						For example:
					</para>

<screen>DASD="eb1c,0.0.a000-0.0.a003,eb10-eb14(diag),0.0.ab1c(ro:diag)"</screen>

					<para>
						For FCP-only environments, remove the <option>DASD=</option> option from the CMS configuration file to indicate no DASD is present.
					</para>
				</listitem>

			</varlistentry>
			<varlistentry>
				<term><parameter>FCP_<replaceable>n</replaceable>="<replaceable>device_bus_ID</replaceable> <replaceable>WWPN</replaceable> <replaceable>FCP_LUN</replaceable>"</parameter></term>
				<listitem>
					<para>
						Where:
					</para>
					<itemizedlist>
						<listitem>
							<para>
								<replaceable>n</replaceable> is typically an integer value (for example <literal>FCP_1</literal> or <literal>FCP_2</literal>) but could be any string with alphabetic or numeric characters or underscores.
							</para>

						</listitem>
						<listitem>
							<para>
								<replaceable>device_bus_ID</replaceable> specifies the device bus ID of the FCP device representing the <firstterm>host bus adapter</firstterm> (HBA) (for example <literal>0.0.fc00</literal> for device fc00).
							</para>

						</listitem>
						<listitem>
							<para>
								<replaceable>WWPN</replaceable> is the world wide port name used for routing (often in conjunction with multipathing) and is as a 16-digit hex value (for example <literal>0x50050763050b073d</literal>).
							</para>

						</listitem>
						<listitem>
							<para>
								<replaceable>FCP_LUN</replaceable> refers to the storage logical unit identifier and is specified as a 16-digit hexadecimal value padded with zeroes to the right (for example <literal>0x4020400100000000</literal>).
							</para>

						</listitem>

					</itemizedlist>
					<para>
						These variables can be used on systems with FCP devices to activate FCP LUNs such as SCSI disks. Additional FCP LUNs can be activated during the installation interactively or by means of a Kickstart file.  An example value looks similar to the following:
					</para>

<screen>FCP_1="0.0.fc00 0x50050763050b073d 0x4020400100000000"</screen>
					<important>
						<para>
							Each of the values used in the FCP parameters (for example <literal>FCP_1</literal> or <literal>FCP_2</literal>) are site-specific and are normally supplied by the FCP storage administrator.
						</para>
					</important>

				</listitem>

			</varlistentry>

		</variablelist>
		<para>
			The installation program prompts you for any required parameters not specified in the parameter or configuration file except for FCP_n.
		</para>

	</section>

	<section id="sect-parameter-configuration-files-kickstart-s390">
		<title>Parameters for Kickstart Installations</title>
		<indexterm>
			<primary>parameter files</primary>
			<secondary>Kickstart parameters</secondary>
		</indexterm>
		<indexterm>
			<primary>Kickstart</primary>
			<secondary>parameters for System z parameter files</secondary>
		</indexterm>
		<para>
			The following parameters can be defined in a parameter file but do not work in a CMS configuration file.
		</para>
		<variablelist>
			<varlistentry>
				<term><parameter>inst.ks=<replaceable>URL</replaceable></parameter></term>
				<listitem>
					<para>
						References a Kickstart file, which usually resides on the network for Linux installations on System&nbsp;z. Replace <replaceable>URL</replaceable> with the full path including the file name of the Kickstart file. This parameter activates automatic installation with Kickstart. See <xref linkend="list-boot-options-kickstart" /> and <xref linkend="sect-kickstart-installation-starting" /> for more details.
					</para>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term><parameter>RUNKS=<replaceable>value</replaceable></parameter></term>
				<listitem>
					<important>
						<para>
							This parameter is deprecated. If you use it in a Kickstart file, it will be ignored. Only the <parameter>inst.ks=</parameter> parameter is necessary to start a Kickstart installation on IBM System&nbsp;z.
						</para>
					</important>
					<para>
						Where <replaceable>value</replaceable> is defined as <replaceable>1</replaceable> if you want to run the loader automatically on the Linux console without having to log in over the network with SSH. To use <literal>RUNKS=1</literal>, the console must either support full-screen or the <parameter>inst.cmdline</parameter> option (below) should be used. The latter applies for the 3270 terminal under z/VM or the operating system messages console for LPAR. We recommend <literal>RUNKS=1</literal> for fully automatic installations with Kickstart. When <literal>RUNKS=1</literal> is set, the installation program automatically continues in case of parameter errors and does not interrupt unattended installations by prompting for user interaction.
					</para>
					<para>
						Leave out the parameter or specify <literal>RUNKS=0</literal> otherwise.
					</para>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term><parameter>inst.cmdline</parameter></term>
				<listitem>
					<para>
						When this option is specified, output on line-mode terminals (such as 3270 under z/VM or operating system messages for LPAR) becomes readable, as the installation program disables escape terminal sequences that are only applicable to UNIX-like consoles. This requires installation with a Kickstart file that answers all questions, because the installation program does not support interactive user input in cmdline mode.
					</para>
				</listitem>
			</varlistentry>

		</variablelist>
		<para>
			Ensure that your Kickstart file contains all required parameters before you use the <parameter>inst.cmdline</parameter> option. If a required command is missing, the installation will fail. See <xref linkend="chap-kickstart-installations" /> for details.
		</para>

	</section>

	<section id="sect-parameter-configuration-files-other-s390">
		<title>Miscellaneous Parameters</title>
		<para>
			The following parameters can be defined in a parameter file but do not work in a CMS configuration file.
		</para>
		<variablelist>
			<varlistentry>
				<term><parameter>rd.live.check</parameter></term>
				<listitem>
					<para>
						Turns on testing of an ISO-based installation source; for example, when booted from an FCP-attached DVD or using <parameter>inst.repo=</parameter> with an ISO on local hard disk or mounted with NFS.
					</para>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term><parameter>nompath</parameter></term>
				<listitem>
					<para>
						Disables support for multipath devices.
					</para>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term><parameter>proxy=[<replaceable>protocol</replaceable>://][<replaceable>username</replaceable>[:<replaceable>password</replaceable>]@]<replaceable>host</replaceable>[:<replaceable>port</replaceable>]</parameter></term>
				<listitem>
					<para>
						Specify a proxy to use with installation over HTTP, HTTPS, or FTP.
					</para>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term><parameter>inst.rescue</parameter></term>
				<listitem>
					<para>
						Boot into a rescue system running from a RAM disk that can be used to fix and restore an installed system.
					</para>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term><parameter>inst.stage2=<replaceable>URL</replaceable></parameter></term>
				<listitem>
					<para>
						Specifies a path to an <filename>install.img</filename> file instead of to an installation source. Otherwise, follows the same syntax as <parameter>inst.repo=</parameter>. If <parameter>inst.stage2</parameter> is specified, it typically takes precedence over other methods of finding <filename>install.img</filename>. However, if <application>Anaconda</application> finds <filename>install.img</filename> on local media, the <parameter>inst.stage2</parameter> URL will be ignored.
					</para>
					<para>
						If <parameter>inst.stage2</parameter> is not specified and <filename>install.img</filename> cannot be found locally, <application>Anaconda</application> looks to the location given by <parameter>inst.repo=</parameter> or <parameter>method=</parameter>.
					</para>
					<para>
						If only <parameter>inst.stage2=</parameter> is given without <parameter>inst.repo=</parameter> or <parameter>method=</parameter>, <application>Anaconda</application> uses whatever repos the installed system would have enabled by default for installation.
					</para>
					<para>
						Use the option multiple times to specify multiple HTTP, HTTPS or FTP sources. The HTTP, HTTPS or FTP paths are then tried sequentially until one succeeds:
<screen>inst.stage2=host1/install.img inst.stage2=host2/install.img inst.stage3=host3/install.img</screen>
					</para>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term><parameter>inst.syslog=<replaceable>IP/hostname</replaceable>[:<replaceable>port</replaceable>]</parameter></term>
				<listitem>
					<para>
						Sends log messages to a remote syslog server.
					</para>
				</listitem>
			</varlistentry>

		</variablelist>
		<para>
			The boot parameters described here are the most useful for installations and trouble shooting on System&nbsp;z, but only a subset of those that influence the installation program. See <xref linkend="chap-anaconda-boot-options" /> for a more complete list of available boot parameters.
		</para>

	</section>

	<section id="sect-parameter-configuration-files-samples-s390">
		<title>Sample Parameter File and CMS Configuration File</title>
		<indexterm>
			<primary>parameter files</primary>
			<secondary>sample parameter file</secondary>

		</indexterm>
		<indexterm>
			<primary>CMS configuration files</primary>
			<secondary>sample CMS configuration file</secondary>

		</indexterm>
		<para>
			To change the parameter file, begin by extending the shipped <filename>generic.prm</filename> file.
		</para>
		<para>
			Example of <filename>generic.prm</filename> file:
		</para>

<screen>ro ramdisk_size=40000 cio_ignore=all,!condev
CMSDASD="191" CMSCONFFILE="redhat.conf"
vnc
inst.repo=http://example.com/path/to/repository</screen>

			<para>
				Example of <filename>redhat.conf</filename> file configuring a QETH network device (pointed to by <parameter>CMSCONFFILE</parameter> in <filename>generic.prm</filename>):
			</para>

<screen>
NETTYPE="qeth"
SUBCHANNELS="0.0.0600,0.0.0601,0.0.0602"
PORTNAME="FOOBAR"
PORTNO="0"
LAYER2="1"
MACADDR="02:00:be:3a:01:f3"
HOSTNAME="foobar.systemz.example.com"
IPADDR="192.168.17.115"
NETMASK="255.255.255.0"
GATEWAY="192.168.17.254"
DNS="192.168.17.1"
SEARCHDNS="systemz.example.com:example.com"
DASD="200-203"
</screen>

		</section>

	</chapter>
